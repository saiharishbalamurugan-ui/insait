// Simple flags for features that are built but not ready to expose yet.
// Flip back to true once the underlying automation actually exists — the
// page/nav-item code is intentionally left in place, not deleted.
export const FEATURES = {
  emailInbox: false,
};
